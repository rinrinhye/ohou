export function addClassList(target, className) {
  target.classList.add(className);
}
export function removeClassList(target, className) {
  target.classList.remove(className);
}

export function toggleClassList(target, className) {
  target.classList.toggle(className);
}
